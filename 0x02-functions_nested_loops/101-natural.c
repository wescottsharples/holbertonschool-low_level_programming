#include <stdio.h>

/**
 * main - check the code for Holberton School students.
 *
 * Return: Always 0.
 */
int main(void)
{
	int n;
	int b;
	int c;

	n = 1024;
	j = 0;

	for (a = i; a <= n; a++)
	{
		if (a % 3 == 0 || a % 5 == 0)
		{
			j = (j + i);
			printf("%d\n", j);
		}
	}
}
