/**
 * _abs - prints the absolute value of input.
 * @i: variable that stands for an integer input
 * Return: Absolute value of i.
 */
int _abs(int i)
{
	if (i < 0)
	{
		i = i * (-1);
		return (i);
	}
	else
	{
		return (i);
	}
}
