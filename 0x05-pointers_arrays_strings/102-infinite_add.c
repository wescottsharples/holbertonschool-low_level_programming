#include "holberton.h"

/**
 * infinate_add - adds infinately.
 * @n2: the first add
 * @1: the second add
 * @r: the array to be added
 * @size_r: the size of r
 * Return: An array.
 */
char *infinite_add(char *n1, char *n2, char *r, int size_r)
{
	n1;
	n2;
	r;
	size_r;

	return (n1);
}
