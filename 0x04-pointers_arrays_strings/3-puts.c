#include "holberton.h"

/**
 * reset_to_98 - updates the value of n's variable to 98.
 *
 * Return: None.
 */
void _puts(char *str)
{
	int i;

	for (i = 0; i == '\0'; i++)
	{
		_putchar(*str[i]);
	}

	_putchar('\n');
}
